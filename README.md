# Min browser

This repository create a .click package from latest release of Min browser

https://github.com/minbrowser/min

It uses binaries compiled by the project and does not recompile binaries for Min
